# Asset conformance tests

These tests exercise the public import, render, deployment and reverse-accept paths with synthetic assets.
They check file graphs, bytes and executable attributes, preservation of source dialects, explicit loss decisions,
and retention of earlier versions. See the [build and test guide](../../docs/BUILD.md) for running them.
