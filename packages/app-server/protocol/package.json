{
    "name": "@oaam/app-server-protocol",
    "version": "0.1.0",
    "description": "Browser-safe strict wire contract for OAAM Clients and the Production Host",
    "license": "Apache-2.0",
    "type": "commonjs",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage"
    },
    "devDependencies": {
        "@vitest/coverage-istanbul": "^2.1.9",
        "typescript": "^5.5.0",
        "vitest": "^2.0.0"
    },
    "engines": {
        "node": ">=22.0.0"
    }
}
