{
    "name": "@oaam/client-framework",
    "version": "0.1.0",
    "description": "Transport-neutral Client connection coordinator for the OAAM Client Protocol",
    "license": "Apache-2.0",
    "type": "commonjs",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "test:coverage": "vitest run --coverage"
    },
    "dependencies": {
        "@oaam/app-server-protocol": "^0.1.0"
    },
    "devDependencies": {
        "@vitest/coverage-istanbul": "^2.1.9",
        "typescript": "^5.5.0",
        "vitest": "^2.0.0"
    },
    "engines": {
        "node": ">=22.0.0"
    }
}
